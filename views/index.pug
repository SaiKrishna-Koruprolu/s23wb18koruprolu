extends layout

block content
  h1= assignment10
  p Welcome to koruprolu assignment #{assignment10}
  <input class="btn btn-secondary" type="button" value="Press Me">
  <br/>
  
  <br>
  div(id="reduce") Reduce the value
  
  <br/>
  script.
     var count = 20;
    var change = -1;
    function doreduce(){
    count += change;
    change -= 1;
    document.getElementById("reduce").innerHTML=`Count ${count} with change ${change}`;
    }
  button(type="button" onclick="doReduce()") Reduce