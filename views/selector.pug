extends layout

block content
    h1= title
    - var image_names = ['1', '2', '3', '4', '5'];

    - for (let i = 0; i < 3; i++) {
        - var img=Math.floor(Math.random()*5)
        - var ship =image_names[img]
        img(src=`images/${ship}.jpg`,alt=`${ship}`,height=300,width=300)
    - }
